spring:
  application:
    name: gateway
  cloud:
    config:
      fail-fast: true
      password: helloWorld
      uri: http://127.0.0.1:8880
      username: user
  main:
    web-application-type: reactive
