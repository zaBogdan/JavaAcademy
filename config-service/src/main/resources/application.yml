spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared/docker
  profiles:
    active: native
  security:
    user:
      password: 'helloWorld'

server:
  port: 8880
